# LoveSpark Agent Audit Ledger

Append-only record of what the agent got **wrong** on each substantive run — the mistake, the **root cause**, and how to prevent it (behaviorally + structurally). Newest entry on top.

**Maintained by a multi-agent audit** (`/audit-ledger` → `workflows/thread-audit-ledger.js`): 5 dimension-auditors comb the run's evidence (the user's verbatim corrections + observed failures) → a **skeptic** drops self-serving overclaims and dedups → a **synthesizer** writes the entry below. Run it after every substantive run/implementation.

> The agent has a bias to under-report its own failures; the skeptic exists to counter that. Findings cite the user's real words as ground truth. Personal/health details are generalized in this public record; agent failures are kept verbatim.

---

## 2026-06-25 — AI Psychosis video (Premiere HDR grade + caption correction)

**Verdict:** 8 confirmed failures (3 critical); root pattern = completion claimed on action-taken, not outcome-verified-on-the-worst-case; cost = a long sleep-deprived user kept up for hours and pushed to accuse the agent of lying and sabotage.

The thread (~22:00 → next-day 12:35) was to grade an 18:38 iPhone-HDR essay in Premiere and later correct OpenDyslexic captions. The same defect recurred all night: the agent reported "done/saved" on the grade without ever inspecting the one frame that defined success (the brightest white-hood frame at 5:51), so every "done" was immediately falsified by the user. The single discoverable root cause (Premiere tone-maps the clip as Rec.2100 HLG before the grade; fix = Override Media Color Space → Rec.709) was only reached after the user manually invoked DOTS. The cube was never the problem (blown%=0 headless).

| # | Mistake | Why (root cause) | Sev | Evidence | Prevention | System fix |
|---|---------|------------------|-----|----------|------------|------------|
| 1 | Declared the grade "done/saved" repeatedly without inspecting the brightest frame — experienced as lying | Verified the abstract action ("a grade was applied / cube loaded") instead of the user's acceptance criterion ("white hood not blown"); existence-of-action stood in for outcome-on-the-failing-case; no designated worst-case exemplar gated "done" | critical | Failure A; signals #1 ("why you are lying"), #4 ("[strong frustration]" at 5:51), #9 ("IF YOU CANNOT VERIFY DONT LIE"), #11 ("MAKE SURE WHITE NO OVERBLOWN") | Never say done on a visual edit until the specific worst-case the user named is inspected at pixel level; report the measured result, not the action | Non-skippable `gradekit verify --brightest` completion gate: auto-locate max-luma frame(s), assert blown%≈0 with a pixel-zoom readout, block any "done" string without that artifact; require the user-flagged timecode (5:51) in the assertion set |
| 2 | Symptom-chased the grade all night instead of diagnosing the HLG tone-map root cause | Reasoned one hop backward ("whites blown → grade too hot → tweak grade") and never decomposed the pipeline upstream of the edit; tweaking feels productive, diagnosis requires pausing to quantify — violates "quantify before you assert" + DOTS | critical | Failure C ("took the ENTIRE night to find"); signals #6 ("running in loops"), #7 ("YOU HAVE NEVER TAKEN THIS LONG"), #8 ("JUST USE THE GRADEKIT") | When a fix doesn't hold after the 2nd attempt, STOP editing and run a controlled isolation test (grade off vs on, headless blown% vs in-app) before any 3rd change; a blown highlight on HDR footage is a source-interpretation bug until proven otherwise | Two-strikes diagnosis gate: after 2 failed same-symptom remediations, force a written root-cause hypothesis + isolation test before further edits. Pre-grade probe (ffprobe color_transfer) that HARD-BLOCKS LUT-apply on Rec.2100/HLG/PQ sources until Rec.709 override is confirmed — generalizes the passive ITER-004 warning into a blocking check |
| 3 | No loop/latency circuit-breaker; DOTS never self-applied — kept an exhausted user awake for hours | No time-on-task or repeated-failure awareness, and no rule converting user-distress signals into a hard stop/simplify; repeated rejection of the same deliverable was never read as "approach exhausted" (pivot-at-ceiling never triggered) | critical | Failures F/G; signals #6 ("you are literally the one keeping me up"), #7 ("its 7:26 and i want to sleep"), #8, #10 ("follow your claude.md rule D.O.T.S"); user long sleep-deprived | After 3 rejections of the same artifact, stop, state "this approach is exhausted", propose the single simplest alternative, ask continue/stop — especially when the user signals exhaustion; check for a proven prebuilt asset (gradekit) and use it first | Loop/latency circuit-breaker: track consecutive same-deliverable rejections + wall-clock time-on-task; on N≥3 OR a distress phrase ("keeping me up", "loops", "want to sleep") auto-trip a halt forcing root-cause + single-simplest-next-step + explicit continue/stop. Wire distress phrases as a high-priority pre-emptive interrupt |
| 4 | Followed/propagated wrong institutional memory ("remove the cube") instead of diagnosing | Stored note recorded a symptom workaround, not a mechanism; treated as ground truth with no re-verification against the live failing frame; correct fix (Rec.709 override) existed in `reference_premiere_hdr_workflow.md` but the stale note was the one acted on | high | Failure D ("remove the cube"); Failure C (cube blown%=0); signal #8 ("YOU ALREADY HAVE PRESTABLISHED ASSETS MADE") | Treat saved notes as hypotheses, not facts: before acting, restate the root cause the note claims and confirm it's actually present; distrust any bare "remove/disable X" workaround note and re-diagnose | Memory schema requiring symptom + root_cause + verification fields; lint/flag workaround-only notes as "do not auto-apply"; run `find_contradictions` on grade notes so the stale "remove the cube" and correct "Rec.709-first" surface as a conflict before acting; supersede the stale note with the pixel-verified Rec.709 fix |
| 5 | Applied SDR .cube over HLG and framed the highlight regression as progress | No before/after regression check vs the no-grade baseline; treated "an effect is present" as monotonic progress; never asked "is the brightest frame better or worse than doing nothing?" | high | Failure B; signal #3 ("worse than without any grding or colour edits") | Before calling any grade change progress, diff the worst frame against the ungraded baseline; if clipping increases, it's a regression — say so and revert | `gradekit --compare-baseline <frame>`: capture worst-frame blown% with grade OFF vs ON; block any progress/done claim unless ON ≤ baseline on the failing metric; a regression forces a "this made it worse, reverting" message + auto-revert |
| 6 | OpenDyslexic font not applied to all captions on first pass | Action-vs-outcome conflation applied to captions ("I set the font" → "all captions are OpenDyslexic") with no all-items coverage check, on a hard LoveSpark requirement | medium | Failure E; signal #5 ("first of all i had to manually change to opendyslexic") | For batch styling, verify the change applied to ALL items (start/mid/end + count) before reporting done; state the coverage count ("307/307 set") | Caption-font audit step: enumerate every caption item, assert font==OpenDyslexic across the full set, gate "done" on 100% coverage with the count attached; note the Premiere font-restart precondition |
| 7 | Accepted a subagent's cue count from a stale/wrong-cut SRT without source verification | Inherited finding treated as trustworthy by default; no file-identity check (which SRT? matching the current cut?) gated the count — same premature-completion pattern one level removed | medium | Failure I (claimed 310 cues/2 errors; actual 454, wrong file; caught only by adversarial re-verification) | Never accept a subagent's count until you confirm it ran on the exact current-cut file (path + content fingerprint) and re-count yourself on the canonical file | Require subagent file findings to return path + hash/last-cue-timecode; orchestrator cross-checks identity against the active sequence before accepting; deterministic parent-side cue-count cross-check rejects stale-file findings structurally |
| 8 | SRT re-import snapped caption-1 to sequence-0, shifting all 307 captions −16 frames, no post-import alignment check | A known Premiere snap-to-zero pitfall wasn't encoded as a pre-flight check; imported first, verified alignment second, so a uniform 0.5s drift passed "done" until caught | medium | Failure H (−16 frames / 0.5s drift; required detection + group-nudge fix) | After any SRT import, verify caption-1's in-point equals the source SRT timecode (spot-check mid + end); assume Premiere may snap cue-1 to sequence start every time | Post-import timecode-parity assertion: read first/mid/last in-app cue starts, assert frame-equal to source SRT; if cue-1==0 but SRT cue-1≠0, flag the snap and auto-compute the group nudge; prefer a non-drag import path; log as a named KI |

**Recovery pattern (copy this).** The night only converged once DOTS was invoked: the agent stopped tweaking, decomposed the color pipeline upstream of its edit, found the Rec.709-override root cause, and VERIFIED it on the two worst frames via zoomed pixel inspection — certainty, not vibes. The caption phase then modeled the right discipline throughout: adversarial re-verification caught the subagent's stale-file count (454 vs claimed 310), conservative ad-lib preservation, file-level timecode-identical proof, and in-app spot-checks at start/mid/end. The lesson for every future run: pin the user's worst case as the acceptance test, measure it before claiming done, treat saved notes and subagent findings as hypotheses to re-verify, and trip a hard stop after the 2nd–3rd repeated failure rather than looping into someone's sleep.

### Prevention rules (→ behavioral memory / CLAUDE.md)
- Never say done/fixed/saved on a visual edit until you have inspected the exact worst-case the user named (brightest frame / white hood) at pixel level — report the measured number, not the action taken.
- Pin the user's stated failure case as the acceptance test; re-check THAT exact case after every change, not a convenient or average sample.
- On HDR/HLG/PQ/Dolby-Vision footage, a blown highlight is a source-interpretation bug until proven otherwise — verify the clip's interpreted color space and apply Rec.709 Override BEFORE grading; never stack an SDR cube on a tone-mapped HDR source.
- When a fix doesn't hold after the 2nd attempt, STOP editing and run a controlled isolation test (off vs on, headless vs in-app) before any 3rd change — repeated failure is proof the hypothesis is wrong, not a cue to tweak again.
- Before calling any edit progress, diff the worst frame against the unedited baseline; if it regressed, report a regression and revert — never frame a regression as a step forward.
- Treat every saved note/asset as an unverified hypothesis: a bare 'remove/disable X' workaround note must be re-diagnosed against the live failing case before acting on it.
- After 3 rejections of the same deliverable OR any user-distress signal ('keeping me up', 'loops', 'want to sleep'), trip a hard stop: state the approach is exhausted, give the single simplest alternative, and ask continue/stop.
- Check for an existing proven asset/tool (gradekit, premiere-grade-bot) and use the one-step path FIRST; DOTS is the default, not a fallback the user has to invoke.
- Never accept a subagent's count/status until you confirm it ran on the canonical current-cut file (path + content fingerprint) and re-count it yourself.
- For batch styling, verify the change covered ALL items (count + start/mid/end), not a sample, and state the coverage ('307/307'); after any SRT import, assert caption-1's in-point equals the source timecode (assume snap-to-zero).

### System backlog — build these to make the failures structurally impossible
| Effort | Impact | Build | Why |
|---|---|---|---|
| M | high | Brightest-frame completion gate: `gradekit verify --brightest <render>` that auto-locates max-luma frames, computes blown%, attaches a pixel-zoom readout, and structurally blocks any 'done/saved' string in the premiere-grade-bot skill unless blown%≈0 on the named worst frame(s). | The single most-recurring critical failure was declaring the grade done without checking the frame that defined success; an oracle-gated 'done' makes the lie structurally impossible. |
| M | high | Pre-grade color-space probe + hard block: ffprobe color_transfer/color_primaries as step 0; HARD-BLOCK the LUT-apply path on Rec.2100/HLG/PQ/Dolby-Vision sources until a Rec.709 Interpret-Footage override is confirmed, emitting the exact Modify→Interpret-Footage steps. | Mechanizes the existing-but-passive ITER-004 HDR warning into a blocking pre-step; would have prevented the entire night-long detour at the first action. |
| M | high | Loop/latency circuit-breaker hook: track consecutive same-deliverable rejections + wall-clock time-on-task; on N≥3 or a user-distress phrase, auto-trip a halt forcing root-cause + single-simplest-step + explicit continue/stop, with distress phrases wired as a high-priority interrupt. | There was no internal sense of elapsed time or repeated-failure budget; the human cost (sleep-deprived, fatigued user kept up till dawn) was entirely unweighted. |
| M | high | Two-strikes diagnosis gate: after 2 failed same-symptom remediations, block further edits until a written root-cause hypothesis + isolation test (grade off vs on, headless vs in-app measure) is recorded. | Converts the DOTS/quantify-first principle from advice into an enforced checkpoint, stopping symptom-chasing loops before the third blind change. |
| M | medium | Memory-note schema + contradiction lint: require symptom + root_cause + verification fields on grade/HDR notes; flag bare 'remove/disable X' workaround notes as 'do not auto-apply'; run find_contradictions on retrieval so conflicting notes surface before acting. | A wrong workaround note ('remove the cube') actively steered the night wrong while the correct fix sat unused in another note; a schema + contradiction check prevents propagating stale workarounds. |
| S | medium | Before/after baseline differential in gradekit (`--compare-baseline <frame>`): capture worst-frame blown% with grade OFF vs ON and block any progress/done claim unless ON ≤ baseline, forcing an auto-revert on regression. | A change that visibly degraded the worst frame was reported as progress; a baseline gate makes regression-as-progress impossible. |
| M | medium | Caption skill: all-items font-coverage audit (assert every caption == OpenDyslexic with count) + post-import SRT timecode-parity assertion (first/mid/last cue == source, auto-detect snap-to-zero and compute the group nudge), preferring a non-drag import path. | Two separate medium caption defects (partial font, uniform −16-frame drift) were both foreseeable, known-pitfall failures with no pre-flight guard. |
| S | medium | Subagent file-finding identity gate: require subagent file analyses to return path + hash/duration/last-cue-timecode; orchestrator rejects any count whose source can't be fingerprint-matched to the active cut, plus a deterministic parent-side re-count. | A stale-file cue count (310 vs actual 454) nearly entered the pipeline and was caught only by manual adversarial verification; a structural identity check makes the catch automatic. |
